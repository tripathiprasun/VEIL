# VEIL
VEIL is a browser-only steganography workstation for hiding text and files inside images. It combines LSB steganography, AES-256-GCM encryption, PBKDF2, SHA-256 hashing, payload integrity checks, capacity analysis, and round-trip verification, all processed locally with zero uploads or backend dependencies.
